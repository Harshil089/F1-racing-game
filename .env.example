# ============================================
# F1 Racing Game - Environment Variables
# ============================================

# Redis/KV (Vercel KV or Upstash Redis) - for leaderboard storage
KV_REST_API_URL=
KV_REST_API_TOKEN=
# OR
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ============================================
# SECURITY - Anti-Cheat & Admin Protection
# ============================================

# Secret used to sign game tokens (HMAC-SHA256)
# Generate with: openssl rand -base64 32
# IMPORTANT: Change this from the default in production!
GAME_SECRET=

# Admin API key for sensitive endpoints (/api/leaderboard/clear, /api/leaderboard/debug)
# Generate with: openssl rand -hex 32
# Without this, admin endpoints are blocked in production
ADMIN_API_KEY=
